version: 0.2

phases:
  install:
    commands:
      - echo Entered the install phase...
      - apt-get update -y
      - apt-get install -y zip
      - apt-get install -y awscli
    finally:
      - echo This always runs even if the update or install command fails 

  build:
    commands:
      - echo Entered the build phase...
      - cd /lambda/test-1 && zip test.zip test.py
      - ls 
      - aws s3 cp test.zip s3://test-lambda-code-1/test-1/test.zip
      - aws lambda update-function-code --function-name  arn:aws:lambda:ap-south-1:474532148129:function:test --s3-bucket test-lambda-code-1 --s3-key test-1/test.zip
    finally:
      - echo Installation Completed...
  post_build:
    commands:
      - echo Entered the post_build phase...
      - echo Build completed on `date`